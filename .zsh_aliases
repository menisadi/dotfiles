# Used third-party tools:
# - eza
# - lazygit
# - lazydocker
# - neofetch
# - glow
# - trafilatura
# - newsboat
# - csvlens
# - gping
# - timewarrior
# - spotify_player
# - git-graph
# - yazi
# - ffmpeg
# - bat
# - gum
# - jq
# - llm
# - fzf
# - jira
# - qsv

# Referred tools by me:
# - bejw
# - vibemark

# Referred scripts by me:
# - move_csv
# - since
# - pomo
# - timew_pom
# - graphfold
# - git_compress
# - list_errors
# - missing_docstrings
# - logic_map
# - unused_files
# - nowplaying
# - pgn_analyze

# --- git / docker ---
alias lzg='lazygit'
alias lzd='lazydocker'
alias dclean="docker stop \$(docker ps -aq) && docker rm \$(docker ps -aq) && docker rmi \$(docker images -q)"
alias gitc="git_compress"
alias graph='git-graph --color=always | less -RS'

# --- eza / file listing ---
alias ee='eza --sort=created --icons=auto --group-directories-first'
alias es='eza --sort=extension --icons=auto --group-directories-first'
alias eea='eza -a --sort=created --icons=auto --group-directories-first'

# more versitle ls/eza version of the eza aliases
function e() {
 if [[ "$1" =~ ^[0-9]+$ ]]; then
  eza --sort=modified -r --icons=always --color=always | head -"$1"
 else
  eza --sort=modified -r --icons=always --color=always --group-directories-first "$@"
 fi
}

function el() {
 if [[ "$1" =~ ^[0-9]+$ ]]; then
  eza -l --sort=modified -r --icons=always --color=always | head -"$1"
 else
  eza -l --sort=modified -r --icons=always --color=always --group-directories-first "$@"
 fi
}

# --- neovim ---
alias avim='NVIM_APPNAME=astronvim nvim'
alias kvim='NVIM_APPNAME=kvim nvim'

alias nvims='NVIM_APPNAME=$(find ~/.config -maxdepth 2 -type f -name "init.lua" -exec dirname {} \; | xargs -I {} basename {} | gum choose) nvim'

nvim-health() {
 local section="${1:-}"
 nvim --headless \
  +"checkhealth ${section}" \
  +"lua for _,b in ipairs(vim.api.nvim_list_bufs()) do local n=vim.api.nvim_buf_get_name(b); if n:match('^health://') then for _,l in ipairs(vim.api.nvim_buf_get_lines(b,0,-1,false)) do print(l) end; break end end" \
  +qall 2>&1
}

# --- search / lookup ---
alias igrep="fzf --bind 'change:reload:rg --column --line-number --no-heading --color=always --smart-case {q} || true' --ansi --layout=reverse --header 'Search in files' --delimiter : --preview 'bat --color=always {1} --highlight-line {2}' --preview-window 'up,60%,border-bottom,+{2}+3/3,~3'"

cheat() {
 curl "https://cheat.sh/$1"
}

# --- time tracking ---
alias tw='timew'
alias twt='timew start'
alias twc='timew continue'
alias twp='timew stop'
alias twm='timew summary sunday - eod'

twg() {
 local tags
 tags=$(
  timew tags | tail -n +4 | awk '{print $1}' |
   gum filter --no-limit |
   tr '\n' ' '
 )

 [[ -z "${tags//[[:space:]]/}" ]] && return 0

 timew start ${=tags}
}

alias pom='pomo -w $(( $(tput cols) * 80 / 100 ))'
alias tpm='timew_pom'

# --- reading list / web ---
alias nwb='newsboat'
alias cless='csvlens --color-columns'

wcurl() { trafilatura -u "$1" --output-format txt 2>/dev/null | wc -w; }

websum() {
 trafilatura --markdown -u "$1" |
  llm -m mlx-community/Qwen3-4B-4bit-DWQ-053125 "Summarize this article in markdown. Write one short overview paragraph, then 4-5 bullet points covering the key ideas. Be concise — the entire summary should fit in under 20 lines." |
  glow -
}

# Pick a link from the bejw reading list and read it.
# Usage: bj [-f|--filter] [-l|--list] [-t|--table] [-P|--pager] [-T|--tui] [--read] [query]
bj() {
 local mode="filter" view="pager" include=() url

 while [[ $# -gt 0 ]]; do
  case "$1" in
   -f | --filter) mode="filter" && shift ;;
   -l | --list) mode="list" && shift ;;
   -t | --table) mode="table" && shift ;;
   -P | --pager) view="pager" && shift ;;
   -T | --tui) view="tui" && shift ;;
   --read) include=(--include-read) && shift ;;
   -h | --help)
    echo "Usage: bj [-f|--filter] [-l|--list] [-t|--table] [-P|--pager] [-T|--tui] [--read] [query]"
    echo "  Pick:"
    echo "    -f, --filter  Fuzzy-filter by title; [query] pre-fills the search (default)"
    echo "    -l, --list    Pick from a plain title list"
    echo "    -t, --table   Pick from a column table"
    echo "  View:"
    echo "    -P, --pager   Read in the glow pager (default)"
    echo "    -T, --tui     Read in the glow TUI, with line numbers"
    echo "  Other:"
    echo "        --read    Include links already marked as read"
    return 0
    ;;
   *) break ;;
  esac
 done

 case "$mode" in
 table)
  url=$(bejw list "${include[@]}" --format csv | gum table | awk -F',' '{print $NF}')
  ;;
 list | filter)
  local picker=(gum choose)
  [[ "$mode" == "filter" ]] && picker=(gum filter --value "$*")
  url=$(bejw list "${include[@]}" --format jsonl |
   jq -r '[.title, .url] | @tsv' |
   "${picker[@]}" |
   cut -f2)
  ;;
 esac

 if [[ -z "$url" ]]; then
  echo "No URL selected."
  return 1
 fi

 if [[ "$view" == "tui" ]]; then
  webread "$url" | glow -t -l
 else
  webread "$url" | glow -p
 fi
}

bejwc() {
 bejw list --format jsonl |
  jq -r '[.url, .title] | @tsv' |
  while IFS=$'\t' read -r url title; do
   words=$(trafilatura -u "$url" --output-format txt 2>/dev/null | wc -w | tr -d ' ')
   printf "%6s  %s\n" "$words" "$title"
  done
}

# --- weather ---
alias wth="curl -s 'wttr.in/{Yeruham,Tel+Aviv,Ein+Hacarmel,Chicago,Baltimore}?format=%l:+%c+%t++⏰+%T\n' | sed 's/.....$//'"

function weather() {
 if [ "$1" = "-w" ]; then
  curl "wttr.in/Herzliya?format=3"
 elif [ "$1" = "-h" ]; then
  curl "wttr.in/Yeruham?format=3"
 elif [ -z "$1" ]; then
  curl "wttr.in/?format=3"
 else
  echo "Usage: weather [-w|-h]"
  echo "  -w: Get the weather for Herzliya"
  echo "  -h: Get the weather for Yeruham"
 fi
}

# --- network ---
pgc() {
 ping -i 1 google.com | awk -F'time=' '{
		if ($2 ~ /ms/) {
			split($2, a, " ms"); time=a[1]
			if (time < 50)       printf "\033[32mGood \033[0m"
			else if (time < 100) printf "\033[33mFair \033[0m"
			else if (time < 200) printf "\033[38;5;208mBad \033[0m"
			else                 printf "\033[31mFail \033[0m"
		} else printf "\033[31mFail \033[0m"
		fflush()
	}'
}

# --- media ---
alias spp='spotify_player'

# alias to love
alias love="/Applications/love.app/Contents/MacOS/love"

# --- misc scripts (mine) ---
alias nowp="nowplaying"
alias analyze="pgn_analyze"

# --- misc utilities ---
alias vm='vibemark'

# Simple mkdir-cd combo
mkcd() {
 mkdir -p "$1" && cd "$1"
}

record() {
 local default_file="record.log"

 # Check if a command is provided
 if [ -z "$1" ]; then
  echo "No command provided. Usage: run_and_log 'command' [filename]"
  return 1
 fi

 # Get the command to run
 local command="$1"

 # Get the file name if provided, otherwise use the default
 local filename="${2:-$default_file}"

 # Run the command and use tee to write to the file and output to terminal
 eval "$command" | tee "$filename"
}

# yazi shell wrapper
function yy() {
 local tmp="$(mktemp -t "yazi-cwd.XXXXXX")"
 yazi "$@" --cwd-file="$tmp"
 if cwd="$(cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
  builtin cd -- "$cwd"
 fi
 rm -f -- "$tmp"
}

topcmds() {
 local n="${1:-20}"
 fc -ln 1 |
  sed -E 's/^[[:space:]]*//' |
  awk 'NF{
    cmd=$1
    if (cmd=="sudo") cmd=$2
    a[cmd]++
  } END{
    for (c in a) printf "%d\t%s\n", a[c], c
  }' |
  sort -nr | head -n "$n" |
  gum table -p -s $'\t' -c Count -c Command
}

# --- LLM helpers ---
llmshell() {
 local cmd
 cmd=$(llm -m gemini-2.5-flash-lite \
  -s "Reply with the raw shell command only. No explanation, no commentary, no code fences, no backticks." \
  -x \
  "$@")
 echo "Generated Command:"
 echo "$cmd" | gum style --foreground 212 --padding "1 1" --italic

 local choice
 choice=$(gum choose "Run" "Copy" "Cancel")

 case $choice in
 "Run")
  eval "$cmd"
  ;;
 "Copy")
  echo "$cmd" | pbcopy
  echo "Command copied to clipboard."
  ;;
 "Cancel")
  echo "Operation canceled."
  ;;
 esac
}

_llmsh_impl() {
 local model="$1"
 shift
 local cmd
 cmd=$(llm -m "$model" \
  -s "Reply with the raw shell command only. No explanation, no commentary, no code fences, no backticks." \
  -x \
  "$@")
 echo "$ $cmd"
 print -n "[e]xecute  [y]ank  [q]uit: "
 read -rk 1 choice
 echo

 case $choice in
 e) echo && eval "$cmd" ;;
 y) echo "$cmd" | pbcopy && echo "Copied." ;;
 *) echo "Canceled." ;;
 esac
}

llmsh() { _llmsh_impl "gemini-3.5-flash" "$@"; }
ollmsh() { _llmsh_impl "mlx-community/Qwen3-4B-Instruct-2507-4bit" "$@"; }
alias ai=llmsh
alias oai=ollmsh

ask() { llm -m "gemini-3.5-flash" -t terse "$@"; }
oask() { gum spin --show-output --spinner dot --title "Thinking..." -- llm -m mlx-community/Qwen3-4B-Instruct-2507-4bit -t terse -o max_tokens 4096 "$@"; }
oaskf() { gum spin --show-output --spinner dot --title "Thinking..." -- llm -m mlx-community/Qwen3-4B-Instruct-2507-4bit -t terse "$@"; }

# --- jira ---
jel() {
 local parent="$1"
 shift
 local mode="open" list=false

 while [[ $# -gt 0 ]]; do
  case "$1" in
   --all) mode="all"; shift ;;
   --list) list=true; shift ;;
   *) shift ;;
  esac
 done

 local status_flags=()
 [[ "$mode" == "open" ]] && status_flags=(--status "To Do" --status "In Progress")

 if [[ "$list" == true ]]; then
  jira issue list -P "$parent" "${status_flags[@]}" \
   --csv --columns "KEY,STATUS,ASSIGNEE,SUMMARY" 2>&1 |
   qsv table |
   cut -c1-"${COLUMNS:-$(tput cols)}"
  return 0
 fi

 local selected
 selected=$(jira issue list -P "$parent" "${status_flags[@]}" \
  --csv --columns "KEY,SUMMARY,STATUS,ASSIGNEE" 2>&1 |
  gum table -s ',')

 [[ -z "$selected" ]] && return 0

 local key
 key=$(echo "$selected" | cut -d',' -f1)
 jv "$key"
}

jpw() {
 local monday="" label="" mode="open" list=false
 while [[ $# -gt 0 ]]; do
  case "$1" in
   --label) label="$2"; shift 2 ;;
   --all) mode="all"; shift ;;
   --list) list=true; shift ;;
   --help|-h)
    echo "Usage: jpw [YYYY-MM-DD] [--label <label>] [--all] [--list]"
    echo "  YYYY-MM-DD   Monday of the desired week (default: this week)"
    echo "  --label      Restrict to a given Jira label"
    echo "  --all        Include all statuses (default: To Do, In Progress only)"
    echo "  --list       Print results without the interactive picker"
    return 0
    ;;
   -*) shift ;;
   *) monday="$1"; shift ;;
  esac
 done

 [[ -z "$monday" ]] && monday=$(date -v-$(($(date +%u)-1))d +%Y-%m-%d)
 local sunday
 sunday=$(date -j -v+6d -f "%Y-%m-%d" "$monday" +%Y-%m-%d)

 local jql="project IS NOT EMPTY AND assignee = currentUser() AND \"Planned Week\" >= \"$monday\" AND \"Planned Week\" <= \"$sunday\""
 [[ -n "$label" ]] && jql="$jql AND labels = \"$label\""

 local status_flags=()
 [[ "$mode" == "open" ]] && status_flags=(--status "To Do" --status "In Progress")

 if [[ "$list" == true ]]; then
  jira issue list -q "$jql" "${status_flags[@]}" \
   --csv --columns "KEY,STATUS,ASSIGNEE,SUMMARY" 2>&1 |
   qsv table |
   cut -c1-"${COLUMNS:-$(tput cols)}"
  return 0
 fi

 local selected
 selected=$(jira issue list -q "$jql" "${status_flags[@]}" \
  --csv --columns "KEY,SUMMARY,STATUS,ASSIGNEE" 2>&1 |
  gum table -s ',')

 [[ -z "$selected" ]] && return 0

 local key
 key=$(echo "$selected" | cut -d',' -f1)
 jv "$key"
}

jv() {
 jira issue view "$@" --plain 2>&1 | awk '
    { gsub(/\033\[[0-9;]*m/, ""); sub(/[[:space:]]+$/, ""); sub(/^ +/, "") }
    /^-+.*Subtasks/ { if (line) print line; exit }
    /^$/ { if (line) { print line; line="" }; if (!blank) print; blank=1; next }
    { blank=0; line = (line ? line " " $0 : $0) }
    END { if (line) print line }
  '

 local subtasks
 subtasks=$(jira issue view "$@" --raw 2>&1 | jq -r '
    .fields.subtasks[] |
    "\(.key)  \(.fields.summary)  •  \(.fields.priority.name)  •  \(.fields.status.name)"
  ')
 [[ -n "$subtasks" ]] && printf "\nSubtasks\n%s\n" "$subtasks"
}

js() {
 local mode="open" list=false

 while [[ $# -gt 0 ]]; do
  case "$1" in
   --all) mode="all"; shift ;;
   --list) list=true; shift ;;
   *) shift ;;
  esac
 done

 local jql="sprint in openSprints()"
 [[ "$mode" == "open" ]] && jql="$jql AND status != Done"

 if [[ "$list" == true ]]; then
  jira issue list --jql "$jql" \
   --csv --columns "KEY,STATUS,ASSIGNEE,PRIORITY,SUMMARY" 2>&1 |
   qsv table |
   cut -c1-"${COLUMNS:-$(tput cols)}"
  return 0
 fi

 local selected
 selected=$(jira issue list --jql "$jql" \
  --csv --columns "KEY,SUMMARY,STATUS,ASSIGNEE,PRIORITY" 2>&1 |
  gum table -s ',')

 [[ -z "$selected" ]] && return 0

 local key
 key=$(echo "$selected" | cut -d',' -f1)
 jv "$key"
}

jsm() {
 local jql="sprint in openSprints() AND assignee = currentUser() AND status != Done"

 local selected
 selected=$(jira issue list --jql "$jql" \
  --csv --columns "KEY,SUMMARY,STATUS,PRIORITY" 2>&1 |
  gum table -s ',')

 [[ -z "$selected" ]] && return 0

 local key
 key=$(echo "$selected" | cut -d',' -f1)
 jv "$key"
}

jsa() {
 local sprint_id="$1"
 if [[ -z "$sprint_id" ]]; then
  echo "Usage: jsa <SPRINT_ID> [--mine]"
  return 1
 fi

 local assignee_filter=()
 [[ "${2:-}" == "--mine" ]] && assignee_filter=(-a "$(jira me)")

 local keys
 keys=$(jira issue list -p DSI --status "To Do" "${assignee_filter[@]}" \
  --csv --columns "KEY,SUMMARY,ASSIGNEE,PRIORITY" 2>&1 |
  gum choose --no-limit --header "Pick tickets for sprint $sprint_id" |
  cut -d',' -f1 |
  tr '\n' ' ')

 [[ -z "${keys//[[:space:]]/}" ]] && return 0
 jira sprint add "$sprint_id" ${=keys}
}

# --- task / publishing ---
# Create a taskwarrior task for the current git branch.
# project = repo name; tag = Jira key from the branch (hyphen -> underscore).
# Usage: tnew ["description"]  (defaults to the branch name)
tnew() {
 local root repo key
 root=$(git rev-parse --show-toplevel 2>/dev/null) || {
  echo "not in a git repo"
  return 1
 }
 repo=$(basename "$root")
 key=$(git symbolic-ref --short HEAD 2>/dev/null | grep -oE '^[A-Z]+-[0-9]+' | tr '-' '_')
 task add "${*:-$(git symbolic-ref --short HEAD)}" project:"$repo" ${key:+ +"$key"}
}

# Copy an HTML file into the html-dump repo, rebuild the index, and publish it via GitHub Pages.
# Usage: publish_html <path-to-html-file>
publish_html() {
 local repo="$HOME/Code/personal/html-dump"
 local src="$1"

 if [[ -z "$src" || ! -f "$src" ]]; then
  echo "Usage: publish_html <path-to-html-file>"
  return 1
 fi

 local name
 name="$(basename "$src")"

 cp "$src" "$repo/$name" &&
  (
   cd "$repo" &&
    ./generate_index.sh &&
    git add "$name" index.html &&
    git commit -m "Add $name" &&
    git push origin main
  ) &&
  echo "https://menisadi.github.io/html-dump/$name"
}

# --- fzf config ---
export FZF_CTRL_R_OPTS=$'
  --no-multi-line
  # only 5 lines height
  --height=15
  --layout=reverse
  --border=rounded
  --info=inline-right
  --prompt="󰋚 history ❯ "
  --pointer="▶ "
  --marker="󰄬 "
  --header="enter: paste • ctrl-/: preview • esc: cancel"
  --bind=ctrl-/:toggle-preview,alt-/:toggle-wrap
  --preview-window=down:80%:wrap:hidden
  --preview=\'echo {} | bat --language=bash --style=plain --color=always\'
'
